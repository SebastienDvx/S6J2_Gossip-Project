<% if !user_signed_in? %>
  <h1>Venez poster tous vos Gossips :D</h1>
  <div class="container">
    <%= link_to("Sign Up", new_user_registration_path, class: 'btn btn-success') %>
    <%= link_to("Log In", new_user_session_path, class: 'btn btn-primary') %>

  </div>
<% else %>
  <h1>Venez poster tous vos Gossips :D</h1>
  <%= link_to("Les Gossips !!", gossips_path, class: 'btn btn-info') %>

<% end %>
